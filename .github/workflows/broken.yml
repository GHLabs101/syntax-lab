name: Build-Test-Deploy

on:
  push:
    branches: [main]

jobs:
  monolith:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "build"
      - name: Test
        run: echo "test"
      - name: Deploy
        run: echo "deploy"
